ext {
    minSdkVersion = 23
    compileSdkVersion = 35
    targetSdkVersion = 35
    androidxActivityVersion = '1.9.2'
    androidxAppCompatVersion = '1.7.0'
    androidxCoordinatorLayoutVersion = '1.2.0'
    androidxCoreVersion = '1.15.0'
    androidxFragmentVersion = '1.8.4'
    coreSplashScreenVersion = '1.0.1'
    androidxWebkitVersion = '1.12.1'
    junitVersion = '4.13.2'
    androidxJunitVersion = '1.2.1'
    androidxEspressoCoreVersion = '3.6.1'
    cordovaAndroidVersion = '10.1.1'
    // Matches @capacitor/push-notifications' own fallback
    // (node_modules/@capacitor/push-notifications/android/build.gradle),
    // pinned here so the app module's own FirebaseApp reference (in
    // PushAvailabilityBridge.java, needed at compile time -- the plugin
    // module's own "implementation" dependency isn't exposed to the app
    // module's compile classpath) resolves the identical version.
    firebaseMessagingVersion = '24.1.0'
}