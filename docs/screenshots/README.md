# Admin Dashboard Screenshots

These screenshots are captured from the isolated demo database.

Current files:

- `dashboard-overview.png`
- `products-list.png`
- `orders-create.png`
- `reports-page.png`
- `invoice-print.png`

To refresh the demo data before capturing new screenshots:

```bash
php artisan demo:refresh
```

Then run the app with:

```env
DB_CONNECTION=demo
```

Demo login:

```txt
demo@example.com
password
```
