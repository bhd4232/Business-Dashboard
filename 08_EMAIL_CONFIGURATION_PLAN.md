# ZamZam ERP — ইমেইল কনফিগারেশন প্ল্যান

তারিখ: ২১ আগস্ট, ২০২৬

## ১. বর্তমান অবস্থা (Current State — কোড অডিটের ভিত্তিতে)

প্রজেক্টের কোড ঘেঁটে দেখা গেছে:

- `.env` ফাইলে বর্তমানে `MAIL_MAILER=log` সেট করা আছে — অর্থাৎ কোনো ইমেইলই আসলে পাঠানো হচ্ছে না, শুধু `storage/logs/laravel.log`-এ লেখা হচ্ছে।
- `MAIL_HOST=127.0.0.1`, `MAIL_USERNAME=null`, `MAIL_PASSWORD=null` — কোনো বাস্তব SMTP/ইমেইল প্রোভাইডারের ক্রেডেনশিয়াল সেট করা নেই।
- `MAIL_FROM_ADDRESS="hello@example.com"` — এটাও ডামি ভ্যালু, রিয়েল ডোমেইনের ইমেইল না।
- `.env.production.example`-এ `MAIL_MAILER=smtp` ধরা আছে কিন্তু `MAIL_HOST`, `MAIL_USERNAME`, `MAIL_PASSWORD` সব খালি — প্রোডাকশনে বসানোর সময় এগুলো পূরণ করা হয়নি।
- `config/mail.php` এবং `config/services.php` আগে থেকেই SMTP, SES, Postmark, Resend, Sendmail সাপোর্ট করার জন্য প্রস্তুত করা আছে (Laravel-এর ডিফল্ট স্ক্যাফোল্ডিং) — অর্থাৎ কোড লেভেলে কাজ বাকি নেই, শুধু ক্রেডেনশিয়াল/কনফিগ দরকার।
- অ্যাপে ইতিমধ্যে ইমেইল-নির্ভর ফিচার আছে:
  - `app/Mail/StorefrontLoginOtp.php` — স্টোরফ্রন্ট লগইন OTP ইমেইল
  - `app/Notifications/BusinessAlert.php` — বিজনেস অ্যালার্ট নোটিফিকেশন (মেইল চ্যানেলসহ হতে পারে)
  - `app/Notifications/AppUpdateAvailable.php` — অ্যাপ আপডেট নোটিফিকেশন
- `QUEUE_CONNECTION=sync` — মানে ইমেইল পাঠানো এখন সিনক্রোনাসভাবে (রিকোয়েস্টের মধ্যেই) হয়, যা প্রোডাকশনে স্লো/ব্লকিং সমস্যা করবে। প্রোডাকশন `.env.production.example`-এ `redis` ধরা আছে কিন্তু আসলে চালু করা হয়নি।

**সারকথা:** কোড রেডি, কিন্তু ইমেইল সার্ভিস কনফিগার করা হয়নি — এই মুহূর্তে OTP, অ্যালার্ট, বা অন্য কোনো সিস্টেম ইমেইল আসলে গ্রাহক/ইউজারের কাছে পৌঁছাচ্ছে না।

---

## ২. লক্ষ্য (Goal)

- একটি নির্ভরযোগ্য ইমেইল প্রোভাইডার সিলেক্ট ও কনফিগার করা যাতে OTP, অ্যালার্ট, ইনভয়েস/রিসিট, পাসওয়ার্ড রিসেট ইত্যাদি ইমেইল সত্যিকারভাবে পৌঁছায়।
- Dev, Staging, Production — তিনটা এনভায়রনমেন্টের জন্য আলাদা কনফিগারেশন স্ট্র্যাটেজি ঠিক করা।
- ইমেইল Queue-এর মাধ্যমে অ্যাসিঙ্ক্রোনাসভাবে পাঠানো নিশ্চিত করা (রিকোয়েস্ট ব্লক না করে)।
- Deliverability (SPF/DKIM/DMARC) ঠিকভাবে সেটআপ করে ইমেইল যেন Spam ফোল্ডারে না যায়।
- মনিটরিং ও এরর হ্যান্ডলিং যোগ করা যাতে ইমেইল ফেইল হলে টের পাওয়া যায়।

---

## ৩. প্রোভাইডার অপশন বিশ্লেষণ

| প্রোভাইডার | সুবিধা | অসুবিধা | কখন উপযুক্ত |
|---|---|---|---|
| **Gmail/Google Workspace SMTP** | কনফিগার সহজ, ব্যবসার নিজস্ব ডোমেইন মেইল থাকলে ভালো | দৈনিক সীমা কম (~500/day), বাল্ক মেইলের জন্য অনুপযুক্ত, ডেলিভারেবিলিটি ট্র্যাকিং নেই | খুব অল্প ভলিউম / টেস্টিং |
| **Postmark** | চমৎকার ডেলিভারেবিলিটি, ট্রানজ্যাকশনাল ইমেইলের জন্য বিশেষায়িত, ভালো এনালিটিক্স, Laravel-এ নেটিভ সাপোর্ট (কোডে আগে থেকেই আছে) | মার্কেটিং ইমেইলের জন্য নিষেধ (শুধু ট্রানজ্যাকশনাল), তুলনামূলক দামি | OTP, রিসিট, অ্যালার্টের মতো ট্রানজ্যাকশনাল মেইল |
| **Amazon SES** | খুব সস্তা (প্রতি ১০০০ মেইলে ~$0.10), হাই ভলিউম হ্যান্ডল করে, কোডে সাপোর্ট আছে | সেটআপ একটু জটিল (ডোমেইন ভেরিফিকেশন, sandbox থেকে বের হতে রিকোয়েস্ট লাগে), AWS নলেজ দরকার | বড় স্কেল, বাজেট-সচেতন প্রোজেক্ট |
| **Resend** | ডেভেলপার-ফ্রেন্ডলি, আধুনিক API, ফ্রি টায়ার ভালো, কোডে সাপোর্ট আছে | তুলনামূলক নতুন সার্ভিস | দ্রুত সেটআপ, ছোট-মাঝারি ভলিউম |
| **Mailgun** | ভালো ডেলিভারেবিলিটি, রিজনেবল দাম | Laravel কোর কনফিগে বাড়তি প্যাকেজ লাগতে পারে | মিডিয়াম ভলিউম |

**সুপারিশ:** যেহেতু `config/services.php`-এ Postmark, SES, Resend — তিনটারই ক্রেডেনশিয়াল স্লট আগে থেকেই রেডি আছে, এর যেকোনো একটা বেছে নিলে কোড পরিবর্তন লাগবে না, শুধু `.env`-এ কী বসাতে হবে।

- ট্রানজ্যাকশনাল-হেভি (OTP, অ্যালার্ট, রিসিট) হলে **Postmark** সবচেয়ে সহজ ও নির্ভরযোগ্য পথ।
- বাজেট/স্কেল প্রায়োরিটি হলে **Amazon SES**।

এই সিদ্ধান্তটা ব্যবসার প্রয়োজন অনুযায়ী চূড়ান্ত করতে হবে — নিচের চেকলিস্টে এটা প্রথম ধাপ হিসেবে রাখা হয়েছে।

---

## ৪. বাস্তবায়নের ধাপ (Step-by-Step Plan)

### ধাপ ১ — প্রোভাইডার সিলেকশন ও অ্যাকাউন্ট তৈরি
- উপরের টেবিল থেকে প্রোভাইডার চূড়ান্ত করা।
- ব্যবসার ডোমেইন ইমেইল (যেমন `noreply@zamzam.com` টাইপ) ঠিক করা — `hello@example.com` ডামি ভ্যালু বদলাতে হবে।
- প্রোভাইডারে অ্যাকাউন্ট খুলে API Key / SMTP ক্রেডেনশিয়াল সংগ্রহ করা।

### ধাপ ২ — ডোমেইন ভেরিফিকেশন ও DNS রেকর্ড
- প্রোভাইডারের ড্যাশবোর্ড থেকে দেওয়া **SPF**, **DKIM**, এবং (ঐচ্ছিক তবে সুপারিশকৃত) **DMARC** রেকর্ড ডোমেইনের DNS-এ যোগ করা।
- এটা ছাড়া মেইল Gmail/Outlook-এ Spam-এ চলে যাওয়ার সম্ভাবনা খুব বেশি।
- DNS প্রোপাগেশন (কয়েক ঘণ্টা লাগতে পারে) শেষে ভেরিফিকেশন কনফার্ম করা।

### ধাপ ৩ — `.env` কনফিগারেশন (Dev/Staging/Production আলাদা)
Postmark উদাহরণ (SES/Resend বাছলে ভ্যালু বদলে যাবে, কাঠামো একই):
```
MAIL_MAILER=postmark
MAIL_FROM_ADDRESS="noreply@yourdomain.com"
MAIL_FROM_NAME="${APP_NAME}"
POSTMARK_TOKEN=xxxxxxxxxxxxxxxx
```
SES উদাহরণ:
```
MAIL_MAILER=ses
MAIL_FROM_ADDRESS="noreply@yourdomain.com"
MAIL_FROM_NAME="${APP_NAME}"
AWS_ACCESS_KEY_ID=xxxx
AWS_SECRET_ACCESS_KEY=xxxx
AWS_DEFAULT_REGION=us-east-1
```
- Dev এনভায়রনমেন্টে `MAIL_MAILER=log` রেখে দেওয়াই ভালো (আসল ইমেইল না পাঠিয়ে লগে দেখা যায়)।
- Staging-এ একটা টেস্ট প্রোভাইডার/ইনবক্স (যেমন Mailtrap) ব্যবহার করা যেতে পারে যাতে ভুল করে রিয়েল ইউজারকে টেস্ট মেইল না চলে যায়।
- Production `.env.production.example`-এর খালি `MAIL_HOST/USERNAME/PASSWORD` (বা প্রোভাইডার-নির্দিষ্ট কী) পূরণ করে সার্ভারের আসল `.env`-এ বসানো।

### ধাপ ৪ — Queue চালু করা (ইমেইল অ্যাসিঙ্ক্রোনাস করা)
- বর্তমানে `QUEUE_CONNECTION=sync`, যার মানে ইমেইল পাঠানোর সময় ইউজারকে অপেক্ষা করতে হয় এবং প্রোভাইডার স্লো হলে পুরো রিকোয়েস্ট আটকে যেতে পারে।
- Production-এ `QUEUE_CONNECTION=redis` চালু করে একটা supervised queue worker (`php artisan queue:work`, Supervisor/Systemd দিয়ে) রান করাতে হবে।
- Mailable/Notification ক্লাসগুলোতে (`StorefrontLoginOtp`, `BusinessAlert`, `AppUpdateAvailable`) `ShouldQueue` ইন্টারফেস যোগ আছে কিনা যাচাই করা, না থাকলে যোগ করা।

### ধাপ ৫ — লোকাল টেস্টিং
- `php artisan tinker` থেকে `Mail::raw(...)` দিয়ে একটা টেস্ট মেইল পাঠানো।
- `StorefrontLoginOtp` ফ্লো ম্যানুয়ালি ট্রিগার করে দেখা ইমেইল ইনবক্সে পৌঁছাচ্ছে কিনা (Spam ফোল্ডারও চেক করা)।
- Mail-tester.com বা GlockApps দিয়ে স্প্যাম স্কোর যাচাই করা।

### ধাপ ৬ — স্টেজিং ডিপ্লয়মেন্ট ও QA
- স্টেজিং সার্ভারে কনফিগ বসিয়ে সব ইমেইল ট্রিগার (OTP লগইন, অ্যালার্ট, আপডেট নোটিফিকেশন) এন্ড-টু-এন্ড টেস্ট করা।
- Queue worker স্টেজিং-এও চালু আছে কিনা নিশ্চিত করা।

### ধাপ ৭ — প্রোডাকশন রোলআউট
- Production `.env`-এ ফাইনাল ক্রেডেনশিয়াল বসানো (কখনোই Git-এ কমিট নয়)।
- Queue worker প্রোডাকশনে Supervisor/Systemd দিয়ে চালু ও অটো-রিস্টার্ট নিশ্চিত করা।
- প্রথম কয়েকদিন প্রোভাইডারের ড্যাশবোর্ড (Bounce/Complaint rate) মনিটর করা।

### ধাপ ৮ — মনিটরিং ও এরর হ্যান্ডলিং
- Failed Queue Job নোটিফিকেশন সেটআপ (Laravel-এর `failed_jobs` টেবিল + Slack/লগ অ্যালার্ট — কোডে `config/services.php`-এ Slack নোটিফিকেশন আগে থেকেই কনফিগার করা আছে, সেটা ব্যবহার করা যায়)।
- Bounce/Complaint হ্যান্ডলিং ওয়েবহুক (প্রোভাইডার সাপোর্ট করলে) যোগ করার কথা বিবেচনা করা যাতে অচল ইমেইল অ্যাড্রেসে বারবার পাঠানো না হয়।

---

## ৫. চেকলিস্ট (সংক্ষেপে)

- [ ] প্রোভাইডার সিদ্ধান্ত (Postmark / SES / Resend / অন্য)
- [ ] ব্যবসার আসল "from" ডোমেইন/ইমেইল ঠিক করা
- [ ] প্রোভাইডার অ্যাকাউন্ট + API Key/SMTP ক্রেডেনশিয়াল
- [ ] SPF/DKIM/DMARC DNS রেকর্ড যোগ ও ভেরিফাই
- [ ] Dev/.env এ log মেইলার বহাল রাখা
- [ ] Staging `.env` কনফিগার (টেস্ট ইনবক্স)
- [ ] Production `.env` কনফিগার (আসল ক্রেডেনশিয়াল)
- [ ] `QUEUE_CONNECTION=redis` + queue worker সেটআপ (Supervisor/Systemd)
- [ ] Mailable/Notification ক্লাসে `ShouldQueue` যাচাই
- [ ] লোকাল টেস্ট মেইল পাঠানো ও স্প্যাম স্কোর চেক
- [ ] স্টেজিং QA (OTP, অ্যালার্ট, আপডেট নোটিফিকেশন)
- [ ] প্রোডাকশন রোলআউট
- [ ] Failed job/Bounce মনিটরিং সেটআপ

---

## ৬. ঝুঁকি ও সতর্কতা

- **ক্রেডেনশিয়াল Git-এ কমিট হওয়া:** `.env` ফাইল `.gitignore`-এ আছে কিনা নিশ্চিত থাকা জরুরি (দেখা গেছে `.gitignore` ফাইল আছে, তবে কনফিগারেশনের সময় ডাবল-চেক করা ভালো)।
- **Sandbox মোড (SES):** নতুন SES অ্যাকাউন্ট ডিফল্টভাবে Sandbox মোডে থাকে, যেখানে শুধু ভেরিফায়েড ইমেইলে পাঠানো যায় — Production Access রিকোয়েস্ট আগে থেকে করে রাখা দরকার (কয়েকদিন সময় লাগতে পারে)।
- **Rate limit:** OTP-ভিত্তিক লগইনে হঠাৎ ট্রাফিক বাড়লে প্রোভাইডারের rate limit-এ যেন না লাগে, তাই প্ল্যান/টায়ার আগে থেকে যাচাই করা।
- **Queue worker ডাউন থাকলে:** `sync`-এর বদলে `redis` ব্যবহার করলে worker প্রসেস বন্ধ থাকলে ইমেইল কিউতে জমে থাকবে, পাঠানো হবে না — তাই Supervisor/Systemd দিয়ে অটো-রিস্টার্ট বাধ্যতামূলক।

---

*এই প্ল্যানটি প্রজেক্ট ফোল্ডারের `.env`, `.env.example`, `.env.production.example`, `config/mail.php`, `config/services.php`, এবং সংশ্লিষ্ট `app/Mail`/`app/Notifications` কোড পর্যালোচনা করে তৈরি করা হয়েছে।*
